# Global defaults shared by every agent in this app.
agent_configuration:
  provider: openai
  timeout: 900
  openai:
    model: gpt-4o

system_tools:
  execute_in_sessions:
    session_pool_management_endpoint: https://pool.example.test
