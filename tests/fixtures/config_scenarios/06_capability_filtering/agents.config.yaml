agent_configuration:
  provider: openai
  timeout: 900
  openai:
    model: gpt-4o

tools:
  exclude:
    - bash
    - execute_shell

system_tools:
  execute_in_sessions:
    session_pool_management_endpoint: https://pool.example.test
