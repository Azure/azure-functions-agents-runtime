agent_configuration:
  provider: openai
  model: gpt-4o
  timeout: 900
  openai: {}

tools:
  exclude:
    - bash
    - execute_shell

system_tools:
  execute_in_sessions:
    session_pool_management_endpoint: https://pool.example.test
