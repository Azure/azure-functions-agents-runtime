agent_configuration:
  provider: openai
  timeout: 600
  openai:
    model: $OPENAI_MODEL
    api_key: $OPENAI_API_KEY

system_tools:
  execute_in_sessions:
    session_pool_management_endpoint: $ACA_SESSION_POOL_ENDPOINT
