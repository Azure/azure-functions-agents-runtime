agent_configuration:
  provider: openai
  model: $OPENAI_MODEL
  timeout: 600
  openai:
    api_key: $OPENAI_API_KEY

system_tools:
  execute_in_sessions:
    session_pool_management_endpoint: $ACA_SESSION_POOL_ENDPOINT
