parameters:
  - name: artifactFeed
    type: string
    default: 'public/PythonSDK_PublicPackages'
  - name: PoolName
    type: string
    default: '1es-pool-azfunc'
  - name: acaServiceConnection
    type: string
    default: 'larohra-sandboxgroup-test'
  - name: acaRuntimeTarget
    displayName: 'Deployed ACA runtime target'
    type: string
    default: both
    values:
      - both
      - python313
      - python314
  - name: acaLoadConcurrency
    displayName: 'Deployed ACA load concurrency'
    type: number
    default: 5
  - name: acaProvisionConcurrency
    displayName: 'Deployed ACA provisioning concurrency'
    type: string
    default: '1'
    values:
      - '1'
      - '2'
      - '4'

jobs:
  # Keep the lower-level harness signal off PRs to retain its existing quota policy.
  - job: "ACAHarnessEntrypointSmoke"
    displayName: 'Run ACA harness entrypoint smoke test'
    condition: in(variables['Build.Reason'], 'Schedule', 'Manual')
    continueOnError: true

    pool:
      name: ${{ parameters.PoolName }}
      image: 1es-ubuntu-22.04
      os: linux

    steps:
      - template: /eng/templates/official/steps/aca-deployed-qualification.yml@self
        parameters:
          artifactFeed: ${{ parameters.artifactFeed }}
          pythonVersion: '3.13'
          serviceConnection: ${{ parameters.acaServiceConnection }}
          displayName: 'Run ACA harness entrypoint smoke test'
          command: harness-suite
      - template: /eng/templates/official/steps/aca-deployed-qualification.yml@self
        parameters:
          artifactFeed: ${{ parameters.artifactFeed }}
          pythonVersion: '3.13'
          serviceConnection: ${{ parameters.acaServiceConnection }}
          displayName: 'Reap ACA smoke sandboxes'
          command: reap-harness-smoke
          condition: always()

  # This validates protected, predeployed environments only. It does not deploy,
  # inspect, or attest the pull request's artifact or its remote Python runtime.
  - job: "ACADeployedColdStart"
    displayName: 'Run predeployed-environment ACA cold-start smoke'
    continueOnError: true
    timeoutInMinutes: 60

    pool:
      name: ${{ parameters.PoolName }}
      image: 1es-ubuntu-22.04
      os: linux

    strategy:
      maxParallel: 2
      matrix:
        ${{ if or(eq(parameters.acaRuntimeTarget, 'both'), eq(parameters.acaRuntimeTarget, 'python313')) }}:
          Python313:
            PYTHON_VERSION: '3.13'
            ACA_DEPLOYED_FUNCTION_BASE_URL: $(ACA_DEPLOYED_PY313_FUNCTION_BASE_URL)
            ACA_DEPLOYED_APP_SITE_NAME: $(ACA_DEPLOYED_PY313_APP_SITE_NAME)
        ${{ if or(eq(parameters.acaRuntimeTarget, 'both'), eq(parameters.acaRuntimeTarget, 'python314')) }}:
          Python314:
            PYTHON_VERSION: '3.14'
            ACA_DEPLOYED_FUNCTION_BASE_URL: $(ACA_DEPLOYED_PY314_FUNCTION_BASE_URL)
            ACA_DEPLOYED_APP_SITE_NAME: $(ACA_DEPLOYED_PY314_APP_SITE_NAME)

    steps:
      - template: /eng/templates/official/steps/aca-deployed-qualification.yml@self
        parameters:
          artifactFeed: ${{ parameters.artifactFeed }}
          pythonVersion: $(PYTHON_VERSION)
          serviceConnection: ${{ parameters.acaServiceConnection }}
          displayName: 'Run predeployed-environment ACA cold-start smoke'
          command: cold-start
          commandArguments: --runtime-target ${{ parameters.acaRuntimeTarget }}

  - job: "ACADeployedAgentTurn"
    displayName: 'Run predeployed-environment ACA agent-turn smoke'
    dependsOn: ACADeployedColdStart
    continueOnError: true
    timeoutInMinutes: 360

    pool:
      name: ${{ parameters.PoolName }}
      image: 1es-ubuntu-22.04
      os: linux

    strategy:
      maxParallel: 2
      matrix:
        ${{ if or(eq(parameters.acaRuntimeTarget, 'both'), eq(parameters.acaRuntimeTarget, 'python313')) }}:
          Python313:
            PYTHON_VERSION: '3.13'
            ACA_DEPLOYED_FUNCTION_BASE_URL: $(ACA_DEPLOYED_PY313_FUNCTION_BASE_URL)
            ACA_DEPLOYED_APP_SITE_NAME: $(ACA_DEPLOYED_PY313_APP_SITE_NAME)
        ${{ if or(eq(parameters.acaRuntimeTarget, 'both'), eq(parameters.acaRuntimeTarget, 'python314')) }}:
          Python314:
            PYTHON_VERSION: '3.14'
            ACA_DEPLOYED_FUNCTION_BASE_URL: $(ACA_DEPLOYED_PY314_FUNCTION_BASE_URL)
            ACA_DEPLOYED_APP_SITE_NAME: $(ACA_DEPLOYED_PY314_APP_SITE_NAME)

    steps:
      - template: /eng/templates/official/steps/aca-deployed-qualification.yml@self
        parameters:
          artifactFeed: ${{ parameters.artifactFeed }}
          pythonVersion: $(PYTHON_VERSION)
          serviceConnection: ${{ parameters.acaServiceConnection }}
          displayName: 'Run predeployed-environment ACA agent-turn smoke'
          command: deployed-suite
          commandArguments: >-
            --runtime-target ${{ parameters.acaRuntimeTarget }}
            --load-concurrency ${{ parameters.acaLoadConcurrency }}
            --provision-concurrency ${{ parameters.acaProvisionConcurrency }}
