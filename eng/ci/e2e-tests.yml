parameters:
  - name: acaServiceConnection
    displayName: 'ACA smoke: Azure service connection'
    type: string
    default: 'larohra-sandboxgroup-test'

trigger:
  batch: true
  branches:
    include:
      - main

pr:
  branches:
    include:
    - main
    - feature/*

schedules:
  - cron: '0 0 * * MON'
    displayName: At 12:00 AM, only on Monday
    branches:
      include:
        - main
    always: true

resources:
  repositories:
    - repository: 1es
      type: git
      name: 1ESPipelineTemplates/1ESPipelineTemplates
      ref: refs/tags/release
    - repository: eng
      type: git
      name: engineering
      ref: refs/tags/release

variables:
  # These are shared YAML variable templates, not Azure DevOps variable groups.
  # Configure every ACA_* value as an ordinary variable directly on this
  # pipeline. Do not add a `group:` dependency for qualification settings.
  - template: ci/variables/build.yml@eng
  - template: ci/variables/cfs.yml@eng

extends:
  template: v1/1ES.Official.PipelineTemplate.yml@1es
  parameters:
    pool:
      name: 1es-pool-azfunc
      image: 1es-windows-2022
      os: windows

    stages:
      - stage: Build
        jobs:
          - template: /eng/templates/official/jobs/build-artifacts.yml@self

      - stage: RunE2ETests
        dependsOn: Build
        jobs:
          - template: /eng/templates/official/jobs/e2e-tests.yml@self
            parameters:
              artifactFeed: 'internal/PythonSDK_Internal_PublicPackages'
              PoolName: 1es-pool-azfunc
              acaServiceConnection: ${{ parameters.acaServiceConnection }}

      # Post-main deployed qualification. Manual runs are allowed on any branch;
      # automatic runs are main-only, excluding PRs and schedules. Keep these
      # conditions local so missing environment configuration fails only the
      # nonblocking ACA work, not pipeline compilation.
      - stage: AcaSweep
        displayName: 'ACA pre-run leak sweep'
        dependsOn: []
        condition: or(eq(variables['Build.Reason'], 'Manual'), and(in(variables['Build.Reason'], 'IndividualCI', 'BatchedCI'), eq(variables['Build.SourceBranch'], 'refs/heads/main')))
        jobs:
          - template: /eng/templates/official/jobs/aca-sweep.yml@self
            parameters:
              acaServiceConnection: ${{ parameters.acaServiceConnection }}
              artifactFeed: 'internal/PythonSDK_Internal_PublicPackages'
              poolName: 1es-pool-azfunc

      - stage: AcaQualification
        displayName: 'ACA deployed qualification'
        dependsOn:
          - Build
          - AcaSweep
        condition: and(succeeded(), or(eq(variables['Build.Reason'], 'Manual'), and(in(variables['Build.Reason'], 'IndividualCI', 'BatchedCI'), eq(variables['Build.SourceBranch'], 'refs/heads/main'))))
        jobs:
          # The job template's matrix runs both runtimes in parallel.
          - template: /eng/templates/official/jobs/aca-qualify.yml@self
            parameters:
              acaServiceConnection: ${{ parameters.acaServiceConnection }}
              artifactFeed: 'internal/PythonSDK_Internal_PublicPackages'
              poolName: 1es-pool-azfunc
